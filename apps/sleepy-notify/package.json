{
  "name": "sleepy-notify",
  "scripts": {
    "build": "cd frontend && bun run build && cd .. && bun run copy-frontend",
    "copy-frontend": "rm -rf backend/public && cp -r frontend/build backend/public",
    "dev:frontend": "cd frontend && bun run dev",
    "dev:backend": "cd backend && bun run dev",
    "preview": "bun run build && cd backend && bun run dev"
  }
}