#!/usr/bin/env bash
# Local overrides for tools-related helper scripts.
# Copy this file to ".env.tools" and adjust values as needed.
#
# This file is NOT loaded automatically by your shell; it is sourced
# only by repo scripts that explicitly read it.

# IP or hostname of the "tools" server running k3s
export TOOLS_SERVER_IP="1.2.3.4"

