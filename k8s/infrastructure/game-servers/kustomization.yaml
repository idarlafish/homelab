---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# game-servers cluster gets all shared Hetzner infrastructure from ../base.
# This file exists so a cluster-specific addition has a clear home in future.
resources:
  - ../base
