---
apiVersion: v1
kind: Service
metadata:
  name: books-nfs
  namespace: booklore
spec:
  selector:
    app: books-nfs
  ports:
    - name: nfs
      port: 2049
      targetPort: nfs
      protocol: TCP
