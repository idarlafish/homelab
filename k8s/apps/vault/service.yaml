---
apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: vault
spec:
  type: ClusterIP
  selector:
    app: vaultwarden
  ports:
    - port: 80
      targetPort: 80
      name: http
