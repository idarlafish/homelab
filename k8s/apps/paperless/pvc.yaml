apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-storage
  namespace: paperless
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: hcloud-volumes-encrypted
  volumeName: pv-paperless-storage
