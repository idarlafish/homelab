---
apiVersion: v1
kind: Service
metadata:
  name: booklore
  namespace: booklore
  labels:
    app: booklore
spec:
  type: ClusterIP
  ports:
  - port: 6060
    targetPort: 6060
    name: http
  selector:
    app: booklore